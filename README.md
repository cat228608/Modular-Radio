# 📻 Mafioznik Radio (AI Powered)

**Mafioznik Radio** — это модульная платформа для создания интернет-радиостанции, управляемой искусственным интеллектом. Система автоматически формирует эфирную сетку, миксуя музыку и работу AI-диджея.

![Web Interface](https://storage.yandexcloud.net/fotora.ru/ec5e5267a5913a8d.png)

![Admin Panel](https://storage.yandexcloud.net/fotora.ru/a4619f0cbc27dcbc.png)

## ✨ Основные возможности

*   🤖 **AI DJ:** Генерация голоса ведущего (Intro/Outro) с использованием нейросетевого синтеза (Edge-TTS).
*   🎵 **Умная ротация:** Поддержка локальной библиотеки и автоматическая загрузка музыки (интеграция с Suno AI/S3).
*   🧩 **Модульность:** Легкое расширение функционала (новости, погода, реклама, счетчики) через Python-плагины.
*   🎛️ **Админ-панель:** Веб-интерфейс для управления расписанием (Drag & Drop), мониторинга логов и настройки модулей.
*   📈 **Легковесность:** Оптимизировано для Ubuntu Server (1 CPU / 1GB RAM).

---

## 🚀 Установка на Ubuntu Server 22.04 / 24.04

### 1. Подготовка системы
Установите системные зависимости: Python, FFmpeg (для кодирования звука), Icecast (сервер вещания) и Nginx (веб-сервер).

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install python3-pip python3-venv ffmpeg icecast2 nginx certbot python3-certbot-nginx -y
```
*> При установке Icecast2 вас попросят ввести пароли. Запомните их (особенно пароль для Source).*

## 🧊 Установка и настройка Icecast (Подробно)

Mafioznik Radio использует **Icecast2** как сервер вещания. Именно он принимает аудио от Python-скрипта и раздает его слушателям.

### 1. Установка пакета
Выполните команду:
```bash
sudo apt install icecast2 -y
```

### 2. Мастер настройки
Во время установки автоматически запустится псевдографический интерфейс (синее окно).
Если окно не появилось, запустите его вручную командой: `sudo dpkg-reconfigure icecast2`.

**Как отвечать на вопросы:**

1.  **Configure Icecast2?** — Выберите **Yes**.
2.  **Hostname:** Введите доменное имя (например, `radio.example.com`) или оставьте `localhost`.
3.  **Source Password:** Пароль для источника вещания.
    *   ⚠️ **Важно:** Запомните этот пароль! Именно его нужно вписать в `config.py` в поле `ICECAST_PASSWORD`.
    *   *Пример: `hackme`*
4.  **Relay Password:** Пароль для ретрансляции. Можно ввести тот же, что и выше.
5.  **Administration Password:** Пароль администратора. Нужен для доступа к веб-интерфейсу самого Icecast (порт 8000).

### 3. Запуск и проверка
Включите автозапуск и запустите сервис:

```bash
sudo systemctl enable icecast2
sudo systemctl start icecast2
```

Чтобы проверить, работает ли сервер, откройте в браузере: `http://IP-ВАШЕГО-СЕРВЕРА:8000`. Вы должны увидеть страницу "Icecast2 Status".

### 4. Тонкая настройка (Опционально)
Конфигурация хранится в файле `/etc/icecast2/icecast.xml`.
Если вы хотите изменить порт (по умолчанию 8000) или увеличить лимит слушателей:

```bash
sudo nano /etc/icecast2/icecast.xml
```

*   `<clients>` — Максимальное число слушателей.
*   `<port>` — Порт вещания.
*   `<admin-password>` — Если забыли пароль админа, его можно подсмотреть здесь.

После изменения файла перезагрузите сервис:
```bash
sudo systemctl restart icecast2
```

### 2. Установка кода
Клонируйте репозиторий в папку `/opt/radio` (рекомендуется):

```bash
cd /opt
sudo git clone https://github.com/cat228608/Modular-Radio.git radio
sudo chown -R $USER:$USER /opt/radio
cd /opt/radio
```

Создайте виртуальное окружение и установите библиотеки:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Настройка конфигурации
Создайте файл конфигурации на основе примера:

```bash
cp config.example.py config.py
nano config.py
```
*> Отредактируйте `ICECAST_PASSWORD`, настройки путей и API ключи внутри файла.*

### 4. Настройка автозапуска (Systemd)
Создайте службу, чтобы радио работало в фоне и автоматически перезапускалось при сбоях.

```bash
sudo nano /etc/systemd/system/mafioznik.service
```

Вставьте следующий контент:

```ini
[Unit]
Description=Mafioznik Radio Orchestrator
After=network.target icecast2.service

[Service]
User=root
WorkingDirectory=/opt/radio
ExecStart=/opt/radio/venv/bin/python3 radio_orchestrator.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

Запустите радио:

```bash
sudo systemctl daemon-reload
sudo systemctl enable mafioznik
sudo systemctl start mafioznik
```

---

## 🌐 Настройка Домена и SSL (Nginx)

Предполагается, что у вас есть домен (например, `radio.example.com`), направленный A-записью на IP вашего сервера.

### 1. Получение SSL сертификата
```bash
sudo systemctl stop nginx
sudo certbot certonly --standalone -d radio.example.com
```

### 2. Конфигурация Nginx
Создайте файл конфигурации сайта. Этот конфиг настроен так, чтобы проксировать запросы к Python-скриптам и потоку вещания.

```bash
sudo nano /etc/nginx/sites-available/radio
```

Вставьте конфиг (не забудьте поменять домен на свой):

```nginx
server {
    server_name radio.example.com; # <-- ВАШ ДОМЕН
    listen 443 ssl http2;

    # Пути к сертификатам (проверьте путь!)
    ssl_certificate /etc/letsencrypt/live/radio.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/radio.example.com/privkey.pem;

    # 1. Главная страница (Веб-плеер)
    # Отдается Nginx напрямую для надежности
    location / {
        root /opt/radio/web;
        index index.html;
        try_files $uri $uri/ =404;
    }

    # 2. Аудио-поток (Icecast)
    location /stream {
        proxy_pass http://127.0.0.1:8000/stream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_buffering off;             # Критично для стриминга
        proxy_request_buffering off;
    }

    # 3. Админ-панель (Python API)
    # Стандартный порт админки: 8080
    location /admin {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    
    # API админки
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # 4. Кастомные модули (Пример: Счетчик онлайна)
    # Если модуль работает на порту 5005, прописываем его явно
    location = /api/heartbeat {
        proxy_pass http://127.0.0.1:5005;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_buffering off;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }
}

# Редирект с HTTP на HTTPS
server {
    listen 80;
    server_name radio.example.com;
    location / { return 301 https://$host$request_uri; }
}
```

Примените настройки:
```bash
sudo ln -s /etc/nginx/sites-available/radio /etc/nginx/sites-enabled/
sudo rm /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
```

Теперь радио доступно по адресу `https://radio.example.com`, а админка — `https://radio.example.com/admin`.

---

## 🧩 Разработка собственных модулей

Архитектура Mafioznik Radio построена на плагинах. Чтобы добавить новую функцию, не нужно лезть в ядро — достаточно создать один файл в папке `modules/`.

### 1. Анатомия модуля
Любой модуль — это Python-класс, наследуемый от `RadioModule`. Создайте файл, например `modules/weather_module.py`:

```python
# /opt/radio/modules/weather_module.py
import random
from .base_module import RadioModule
from logger import log

class WeatherModule(RadioModule):
    def __init__(self):
        super().__init__()

    # 1. Настройки, которые появятся в Админ-Панели
    def get_config_schema(self):
        return {
            "city": {
                "label": "Город для прогноза",
                "type": "text",
                "default": "Moscow"
            },
            "voice": {
                "label": "Голос диктора",
                "type": "select",
                "options": ["male", "female"],
                "default": "female"
            }
        }

    # 2. Логика, выполняемая при попадании в эфир
    def prepare(self, event_config, context):
        # Получаем настройки
        city = self.config.get("city")
        
        # ... тут ваш код (запрос к API погоды, генерация звука) ...
        log(f"Генерирую погоду для {city}")

        # Возвращаем результат Оркестратору
        return {
            "audio_path": "/path/to/weather.mp3", # Путь к готовому файлу
            "meta": {
                "title": "Прогноз погоды",
                "image": "https://example.com/weather.jpg"
            },
            "cleanup": True # Удалить файл после воспроизведения?
        }
```

---

### 2. Типы настроек (Config Schema)
В методе `get_config_schema` вы описываете интерфейс управления модулем. Админ-панель автоматически отрисует форму.

Доступные типы полей:

*   **`text`**: Обычное текстовое поле (API ключи, короткие строки).
*   **`textarea`**: Многострочное поле (Промпты, списки, шаблоны).
*   **`select`**: Выпадающий список. Требует массив `options`.

**Пример сложной конфигурации:**
```python
def get_config_schema(self):
    return {
        "api_key": {
            "label": "OpenWeather API Key",
            "type": "text",
            "default": ""
        },
        "prompt_template": {
            "label": "Шаблон текста (используйте {temp})",
            "type": "textarea",
            "default": "Сейчас в городе {temp} градусов."
        },
        "units": {
            "label": "Единицы измерения",
            "type": "select",
            "options": ["metric", "imperial"],
            "default": "metric"
        }
    }
```

---

### 3. Продвинуто: Модули с Веб-интерфейсом
Если вы хотите создать модуль, который взаимодействует с пользователями сайта (например: **Стол заказов**, **Чат**, **Голосования**), вам нужно запустить веб-сервер внутри модуля.

#### Шаг А: Код модуля (Python)
Используйте `Flask` и запускайте его в отдельном потоке (`threading`), чтобы не блокировать радио.

```python
# modules/request_module.py
from flask import Flask, request, jsonify
import threading
from .base_module import RadioModule

class RequestModule(RadioModule):
    def __init__(self):
        super().__init__()
        self.is_system = True # Системный модуль (работает в фоне)
        self.app = Flask(__name__)
        
        # Определяем роуты
        @self.app.route('/api/request_song', methods=['POST'])
        def request_song():
            song = request.form.get('song_name')
            # ... логика добавления в очередь ...
            return jsonify({"status": "ok", "message": f"Заказано: {song}"})

    def get_config_schema(self):
        return {"port": {"label": "Порт", "type": "text", "default": "5005"}}

    def prepare(self, event_config, context):
        # Запускаем Flask только один раз при старте
        port = int(self.config.get("port", 5005))
        
        def run_server():
            # host='127.0.0.1' - слушаем только локально (безопасность)
            self.app.run(host='127.0.0.1', port=port, use_reloader=False)

        thread = threading.Thread(target=run_server, daemon=True)
        thread.start()
        
        return None # Системный модуль ничего не играет в эфир
```

#### Шаг Б: Настройка Nginx (Обязательно!)
Так как Flask слушает локальный порт (5005), внешний мир его не видит. Нужно "пробросить" его через Nginx.

Откройте конфиг `/etc/nginx/sites-available/radio` и добавьте внутри `server { ... }`:

```nginx
    # Проксирование запросов к вашему модулю
    location /api/request_song {
        proxy_pass http://127.0.0.1:5005; # Порт из настроек модуля
        
        # Передача заголовков (чтобы видеть реальный IP юзера)
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
```
После этого перезагрузите Nginx: `sudo systemctl reload nginx`.
Теперь JS на вашем сайте может слать запросы на `https://radio.mafioznik.xyz/api/request_song`.
